#!/bin/sh /etc/rc.common

START=96
STOP=9
USE_PROCD=1

CONFIG="/etc/mielofon/mielofon-agent.toml"

start_service() {
	procd_open_instance
	procd_set_param command /usr/sbin/mielofon-agent "$CONFIG"
	procd_set_param respawn
	procd_set_param stdout 1
	procd_set_param stderr 1
	procd_close_instance
}